type ResultArr = {
  left: { key: Number; name: String }[]
  middle: { key: Number; name: String }[]
  right: { key: Number; name: String }[]
}

type ReelItem = {
  key: Number
  name: String
}

export type { ResultArr, ReelItem }
